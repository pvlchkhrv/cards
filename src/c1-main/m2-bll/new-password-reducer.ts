const initialState = {};
export const newPasswordReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case '': {
            return state;
        }
        default:
            return state;
    }
};
