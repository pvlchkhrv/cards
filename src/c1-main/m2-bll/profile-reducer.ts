const initialState = {};
export const profileReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case '': {
            return state;
        }
        default:
            return state;
    }
};
