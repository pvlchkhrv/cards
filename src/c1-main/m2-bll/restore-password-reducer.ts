const initialState = {};
export const restorePasswordReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case '': {
            return state;
        }
        default:
            return state;
    }
};
